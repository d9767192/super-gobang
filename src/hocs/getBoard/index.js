import React from 'react';

const getBoard = (Component) => {
  const boardProps = {
    grid: 15,
    width:
  };
  boardProps.space = Math.floor()
  return (props) => <Component {...boardProps} {...props} />;
};

export default getBoard;
